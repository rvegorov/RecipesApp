<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/recipeCardHeight"
    android:layout_marginHorizontal="@dimen/halfMargin"
    android:layout_marginVertical="@dimen/halfMargin"
    android:background="@color/white_color"
    android:clickable="true"
    android:foreground="?selectableItemBackground"
    app:cardCornerRadius="@dimen/shapesRadius">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white_color"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/ivRecipeImage"
            android:layout_width="match_parent"
            android:layout_height="@dimen/recipeCardImageHeight"
            android:contentDescription="@string/description_recipe_image_placeholder"
            android:scaleType="centerCrop"
            tools:src="@drawable/bcg_categories" />

        <TextView
            android:id="@+id/tvRecipeTitle"
            style="@style/StyleCategoryCardTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/halfMargin"
            android:layout_marginTop="@dimen/halfMargin"
            tools:text="@string/title_recipe_placeholder" />
    </LinearLayout>
</androidx.cardview.widget.CardView>